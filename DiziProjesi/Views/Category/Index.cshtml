@model IEnumerable<DiziProjesi.Models.Entity.Category>
@{
    ViewBag.Title = "Kategoriler";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}
<div class="row">
    <div class="col-md-12">
        <h2>@ViewBag.Title</h2>
        <hr />
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <p>
            @Helpers.LinkButton("/Category/Create", "primary", string.Empty, string.Empty, "plus", " Yeni")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Cat_Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Ins_Time)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Up_Time)
                </th>
                <th></th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cat_Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Ins_Time)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Up_Time)
                    </td>
                    <td>
                        @Helpers.LinkButton("/Category/Edit/" + item.ID, "warning", "btn-xs", "Düzenle", "edit", string.Empty)
                        @Helpers.LinkButton("/Category/Delete/" + item.ID, "danger", "btn-xs", "Kaldır", "trash", string.Empty)
                    </td>
                </tr>
            }
        </table>
    </div>
</div>